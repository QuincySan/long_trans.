# 开发记录

## 项目初始化

- 创建项目目录结构
- 初始化Git仓库
- 添加必要的配置文件

## MVP核心功能实现

- Markdown文档的智能分段
- 保持原有Markdown格式的翻译
- 代码块和注释的分别处理
- 可配置的翻译参数
- 友好的命令行接口

## 下一步

- 独立LLM调用组件，方便切换不同的模型。完成
- 添加进度记录，在 terminal 显示翻译进度和内容。包括<think></think>标签内的内容。完成
- 添加错误处理和日志记录。包含原文和对应的翻译结果，按照分块的顺序，保存到本地文件。完成
- 翻译工作流中，对原文先总结，作为上下文给模型，辅助翻译。完成
- 添加批量翻译功能。
  - 文件夹内批量翻译
- 添加 GUI
  - 添加翻译模型选择功能。
- 错误的时候，增加错误日志。
- 对错误的各种处理方式。比如跳过，记录，再尝试等

### 优化想法

- 分块计数换成token
- 批量翻译错误处理
- 外置配置文件
- 优化分块逻辑和策略


## 2025-02-14

- 添加翻译进度显示功能：
  - 显示当前翻译段落数/总段数
  - 实时输出翻译内容
  - 以黄色显示模型思考过程（<think></think>标签）
  - 显示每段原文预览

- 优化模型输出：
  - 添加强制思考过程输出，使用grammar规则确保模型输出格式符合要求

- 重构LLM调用代码：
  - 创建独立的`llm_client.py`模块
  - 实现`LLMClient`基类和`FireworksClient`类
  - 支持多种LLM提供商和模型
  - 优化代码结构，提高可维护性和扩展性
  - 添加`ZetaClient`类，支持Claude 3.5 Sonnet模型
  - 对 translator.py 中的 response_format增加条件判断
  - 将默认翻译模型从DeepSeek-R1切换为Claude 3.5 Sonnet

- 添加翻译日志功能：
  - 创建 `TranslationLogger` 类，以 JSON 格式记录翻译过程
  - 记录每个片段的原文、译文、时间戳和元数据
  - 支持自定义日志目录（默认：logs/）
  - 在 `.gitignore` 中添加 logs/ 目录

- 添加统一的文章管理：
  - 创建 `articles` 目录，用于存放原文和翻译结果
  - 修改代码以支持在 articles 目录下查找输入文件和保存输出文件
  - 在 `.gitignore` 中添加 articles/ 目录

- 优化翻译工作流：
  - 实现两级分段策略：
    - 第一级：约20,000字/段，用于生成摘要
    - 第二级：约5,000字/段，用于实际翻译
  - 为每个大段生成摘要，作为翻译时的上下文
  - 优化日志记录，添加摘要信息到元数据

- 重构代码结构：
  - 添加 `process_large_chunk` 函数处理大段文本
  - 优化 `translate_file` 函数的流程
  - 修复 `TranslationLogger` 的方法调用问题
  - 改进进度显示和日志输出格式

- 优化分段策略：
  - 将分段计数方式从字符(character)改为单词(word)计数
  - 英文按标准单词计数，中文每字算一词
  - 代码块内容不计入统计
  - 更新相关文档说明

- 优化翻译器代码结构：
  - 重构 `Translator` 类，消除代码冗余
  - 抽取公共功能为私有方法：`_build_translation_prompt`、`_process_stream_response`、`_get_response_format`
  - 统一翻译接口，移除冗余方法 `translate_with_summary` 和 `translate_batch_with_summary`
  - 将模型名称改为可配置的默认值
  - 优化日志记录的元数据格式
  - 添加更完善的类型提示

## 2025-02-15

- 优化分段策略：
  - 将二级分段长度从5,000词调整为3,000词，以提高翻译质量
  - 更新相关代码文件中的默认值和注释说明
  - 修改 chunker.py、main.py 中的分段大小设置

- 进一步优化分段策略：
  - 将二级分段长度从3,000词调整为2,000词，进一步提高翻译质量和准确性
  - 更新所有相关文件中的默认值和文档说明
  - 修改 chunker.py、main.py、README 中的分段大小设置

- 优化翻译器提示词结构：
  - 将提示词拆分为 system prompt 和 user prompt
  - system prompt 包含翻译要求和指导原则
  - user prompt 包含待翻译内容和上下文摘要
  - 重构 `_build_translation_prompts` 方法以支持新的提示词结构

- 添加新模型支持：
  - 在 ZetaClient 中添加 SUPPORTED_MODELS 配置，支持三个模型：
    - claude-3-5-sonnet-20241022
    - deepseek-v3
    - gemini-2.0-pro-exp-02-05
  - 为每个模型配置默认参数（max_tokens、temperature）
  - 在 generate_text 中增加模型验证

- 优化模型选择机制：
  - 在 main.py 中添加 --model 命令行参数
  - 更新相关文档，添加模型使用说明
  - 将默认翻译模型从 Claude 3.5 Sonnet 切换为 DeepSeek-V3

- 统一文件处理流程：
  - 移除 articles 目录的概念，统一使用 to_translate 和 translated 文件夹
  - 重构 translate_file 函数，使用完整文件路径
  - 将 process_batch 重命名为 process_files，支持单文件和批量处理
  - 简化命令行参数：
    - 移除 --batch 参数，默认处理整个文件夹
    - 添加 -f/--file 参数用于可选的单文件指定
    - 统一使用 --input-folder 和 --output-folder 参数

- 添加自动创建文件夹功能：
  - 添加 ensure_folders_exist 函数，自动检查和创建必要的文件夹
  - 在程序启动时自动创建 to_translate、translated 和 logs 文件夹
  - 优化错误处理，统一文件夹创建逻辑
  - 将日志目录配置移至全局常量 DEFAULT_LOG_FOLDER

- 添加翻译质量等级功能：
  - 添加 `--quality` 命令行参数，支持 basic 和 high 两个选项
  - 实现基础翻译和高质量翻译两种模式
  - 更新翻译器提示词，针对不同等级优化翻译要求
  - 设置 `python main.py` 默认使用基础翻译等级
  - 更新文档，添加质量等级相关说明和示例

- 添加高级翻译流程：
  - 创建 reviewer.py 模块，实现翻译评分和润色功能
  - 添加 TranslationReviewer 类，支持：
    - 五维度评分：准确性、完整性、流畅性、术语、风格
    - 自动润色：评分低于45分时进行优化
    - JSON格式的评分结果
  - 在高质量翻译模式下集成评分和润色流程
  - 在日志中添加评分信息
  - 优化终端输出，显示评分和润色状态

- 优化翻译工作流：
  - 简化翻译提示词，统一使用基础提示词
  - 将质量控制交由 reviewer 处理
  - 添加 quality_level 参数，支持 basic 和 high 两种模式
  - 更新文档，添加质量等级相关说明

- 优化翻译评审功能：
  - 添加可配置的评分阈值
  - 优化润色逻辑，增加对低分维度的特别关注

- 优化润色流程：
  - 修改 reviewer.py，实现润色过程流式输出
  - 设置不同任务使用不同模型：
    - 评分：claude-3-5-sonnet-20241022
    - 润色：deepseek-v3
  - 添加友好的进度提示
  - 添加 TranslationReviewError 异常类

- 优化评分和润色流程：
  - 简化代码结构，避免重复调用评分和润色
  - 将评分和润色逻辑集中在 translate_text 中
  - 优化日志输出，避免重复提示
  - 在润色提示中显示具体的分数阈值

- 优化翻译质量等级：
  - 将"高级翻译"模式改名为"中级翻译"模式
  - 更新 translator.py 中的 quality_level 参数说明
  - 更新 reviewer.py 中的模块说明
  - 更新 README.md 中的相关说明
  - 保持原有翻译、评分和润色逻辑不变

## 2025-02-16

- 添加高级翻译模式：
  - 创建 `advanced_reviewer.py`，实现详细点评和润色功能
  - 更新 `translator.py`，支持三种翻译等级：basic、medium、advanced
  - 修改命令行参数 `--quality`，支持 advanced 选项
  - 优化日志记录，添加高级模式的审校结果记录

- 优化翻译流程：
  - 简化审校提示词，移除 JSON 格式限制
  - 优化点评和润色逻辑，提供更自然的改进建议
  - 统一日志记录格式，支持文本形式的审校意见

- 优化高级翻译模式：
  - 将高级审校流程改为流式输出
  - 修改 `AdvancedReviewer` 类中的三个主要方法：
    - `comment_on_translation`：支持流式输出审校意见
    - `polish_with_comments`：支持流式输出润色结果
    - `comment_and_polish`：整合流式输出的完整工作流
  - 添加类型提示：Generator、Iterator、Union
  - 保留非流式模式的向后兼容性
  - 优化提示词结构，将 system prompt 和 user prompt 分离
  - 在 `AdvancedReviewer` 类中添加专门的角色定位：
    - 审校专家：负责评审和提供改进建议
    - 中文编辑：负责根据审校意见优化译文
  - 优化提示词内容，使指导更清晰具体
  - 保持原有功能和流程不变，仅优化提示词组织方式

- 优化 AdvancedReviewer：
  - 添加 latest_comments 和 latest_polished_text 属性存储结果
  - 修改 comment_and_polish 方法，统一返回值格式
  - 添加 get_latest_results 方法获取最新结果
  - 优化审校提示词，增加 markdown 格式保持要求

- 优化分块策略：
  - 修改词数统计方式：不再移除代码块，让代码块内容参与词数统计
  - 优化代码块处理：在 _further_split_block 中优先识别代码块，保持代码块完整性
  - 添加 _split_paragraphs 方法专门处理普通文本的分段
  - 重构分块逻辑，确保代码块不被随意切分
  - 优化注释和文档说明

- 优化提示词结构：
  - 将提示词中的"翻译"相关词语统一改为"重写"
  - 修改涉及文件：translator.py、reviewer.py、advanced_reviewer.py
  - 保持代码实现部分（变量名、注释等）不变
  - 确保功能和结构保持一致，仅改变提示词表述
