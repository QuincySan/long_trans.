## 基于 LLM 的 Markdown 文本批量翻译

### 一、项目简介

本项目用于对大段 Markdown 文档进行自动化翻译，力图保留原有的 Markdown 结构。与一般翻译流程不同的是，本项目先对文本进行长文本摘要，再在实际翻译时将摘要内容作为上下文，帮助大语言模型（LLM）更好地理解文本背景并进行准确翻译。

#### 项目主要流程

- 读取并解析 Markdown 文件
- 对全文按「约 2 万词」进行首轮分段（如果原文小于 2 万词，则无需拆分），并对每段进行整体概要。
- 再次细分翻译分段：对原文（或者分段后的每部分）再按「约 2,000 词」进行二次分段，用于实际调用 LLM 翻译。在翻译时，将对应的摘要内容与 2,000 词分段的文本一并传入 LLM，帮助模型理解上下文。
- 将翻译结果拼接回原有的 Markdown 格式，形成最终输出。

#### 示例：
- **2 万词内**：不用拆分，先整体做摘要，再按 2,000 词翻译分段，翻译时带上整体摘要。
- **4 万词**：先拆分为两段，每段 2 万词；对每段做摘要，然后各自再分割成若干个 2,000 词，翻译时带上对应的 2 万词摘要。
- **6 万词**：分为三段，每段 2 万词；对每段做摘要，翻译时各段分别带其对应的摘要。

#### 单词计数说明：
- 字：character
- 词：word
- **英文文本**：按照标准英文单词进行计数（包括带连字符和数字的单词）
- **中文文本**：每个汉字视为一个词进行计数
- **混合文本**：同时支持中英文混合计数
- **特殊处理**：
  - 代码块内容不计入单词统计，避免影响分段
  - 保持 Markdown 格式标记的完整性
  - 优先在自然段落边界进行分段

---

### 二、技术选型

- **语言**：Python（建议版本 3.7+）
- **依赖库**：
  - `openai`（或自定义封装，用于调用 Fireworks.ai 等 LLM 服务）
  - `markdown`、`markdown-it-py` 或类似库（可根据需要进行文本解析和处理）
  - 其他可选的文本处理工具，如 `re`、`textwrap` 等
  - 其他必要的工具库（如 `argparse`、`dotenv` 等）

**注意**：本项目对 `openai` 模块进行了自定义封装，实际使用 Fireworks.ai 等服务。调用示例请参考下文。依然符合Openai协议。

---

### 三、分段与翻译核心思路

#### 1. 第一轮分段（约 2 万词）并摘要
- 读取整个 Markdown 文本，统计词数。
- 若小于 2 万词，则无需拆分，直接对全文做摘要。
- 若超过 2 万词，每 2 万词左右切分一次（推荐以自然段边界切分）。
- 对每个分段调用 LLM 生成段落级摘要。

#### 2. 第二轮分段（约 2,000 词）并翻译
- 对原文或对应的首轮分段再次进行精细切分，每次约 2,000 词左右。
- 每个 2,000 词分段翻译时，将「对应的摘要」和「分段文本」一起作为 Prompt 发给 LLM，以便模型在理解上下文的基础上进行翻译。
- 将翻译结果记录下来，并与原文段落/块建立对应关系。

#### 3. 拼接并保持 Markdown 结构
- 最终将所有翻译后的文本按原顺序拼接，保留标题、列表、代码块等 Markdown 结构。
- 输出完整的翻译后 Markdown 文件。

---

### 四、目录结构

```plaintext
markdown-translator/
│
├── README.md                # 项目说明文档
├── requirements.txt         # 项目依赖库
├── main.py                  # 主入口脚本，组织翻译流程
├── summarizer.py            # 摘要生成：2 万词级分段 + 调用 LLM
├── translator.py            # 翻译处理：2,000 词级分段 + 调用 LLM
├── chunker.py               # 分段策略实现
├── llm_client.py            # LLM 客户端封装 (支持多种模型)
├── to_translate/           # 存放待翻译的 Markdown 文件
├── translated/             # 存放翻译结果和原文件
├── logs/                   # 存放翻译日志
└── utils/
    ├── markdown_parser.py   # Markdown 解析与还原
    └── translation_logger.py # 翻译日志记录
```

---

### 五、示例流程代码片段

#### `summarizer.py`（简化示例）

```python
from llm_client import call_llm

def summarize_long_text(text: str) -> str:
    """
    调用 LLM 对约 2 万词的文本进行摘要。
    返回摘要字符串。
    """
    prompt = (
        "以下是一段较长的文本，请生成一个简要的摘要，"
        "包含主要主题、关键人物、核心概念等：\n\n"
        f"{text}\n\n"
        "请注意：需要中文输出，且摘要应不超过 500 词。"
    )
    summary = call_llm(prompt)
    return summary
```

#### `translator.py`（简化示例）

```python
from llm_client import call_llm

def translate_segment(segment: str, summary: str) -> str:
    """
    调用 LLM 对分段文本进行翻译，summary 为本段对应的前置摘要上下文。
    返回翻译结果。
    """
    prompt = (
        "你是一个专业的翻译助手，请根据下方摘要信息，对后续Markdown文本进行翻译。\n"
        "在翻译时，请尽可能保留Markdown格式，包括标题、列表、代码块等结构。\n\n"
        f"【本段摘要】:\n{summary}\n\n"
        f"【待翻译文本】:\n{segment}\n\n"
        "请注意：仅翻译正文内容，Markdown结构与标记请原样保留。"
    )
    translated = call_llm(prompt)
    return translated
```

#### 3. chunker.py：分段逻辑

此处我们演示了两个函数：
- `split_into_large_chunks(text, max_size=20000)`：按 ~20,000 词分成大块，用于"总结"阶段；
- `split_into_small_chunks(text, max_size=3000)`：按 ~3,000 词分成小块，用于"翻译"阶段。

以下是代码示例：

```python
import re

def split_into_large_chunks(text, max_size=20000):
    """
    按约 20,000 词为单位进行大块拆分，尽量在段落边界断开。
    若总长度 <= 20,000，不拆分；否则拆分多个块。
    返回一个列表，每个元素是一段文本。
    """
    # 去掉多余空行，以便后续处理
    paragraphs = [p.strip() for p in text.split('\n') if p.strip()]

    chunks = []
    current_chunk = []
    current_length = 0

    for para in paragraphs:
        para_length = len(para)
        # 如果加上这个段落后超过max_size，则先把当前块加入chunks，再新建块
        if current_length + para_length > max_size and current_chunk:
            chunks.append("\n".join(current_chunk))
            current_chunk = [para]
            current_length = para_length
        else:
            current_chunk.append(para)
            current_length += para_length
    
    # 最后一块
    if current_chunk:
        chunks.append("\n".join(current_chunk))

    return chunks


def split_into_small_chunks(text, max_size=3000):
    """
    按约 3,000 词为单位进行小块拆分，尽量在段落边界断开。
    返回一个列表，每个元素是一段文本。
    """
    paragraphs = [p.strip() for p in text.split('\n') if p.strip()]

    chunks = []
    current_chunk = []
    current_length = 0

    for para in paragraphs:
        para_length = len(para)
        if current_length + para_length > max_size and current_chunk:
            chunks.append("\n".join(current_chunk))
            current_chunk = [para]
            current_length = para_length
        else:
            current_chunk.append(para)
            current_length += para_length

    if current_chunk:
        chunks.append("\n".join(current_chunk))

    return chunks
```


#### 6. main.py：主流程示例

```python
import os
from chunker import split_into_large_chunks, split_into_small_chunks
from summarizer import summarize_text
from translator import translate_segment

def main(input_file, output_file):
    # 1. 读取 Markdown 文件
    with open(input_file, "r", encoding="utf-8") as f:
        raw_text = f.read()
    
    text_length = len(raw_text)
    
    # 2. 判断是否需要拆分大块
    if text_length <= 20000:
        large_chunks = [raw_text]
    else:
        large_chunks = split_into_large_chunks(raw_text, max_size=20000)
    
    all_translated_texts = []
    
    # 3. 对每个大块分别进行总结，然后再拆分小块进行翻译
    for idx, chunk in enumerate(large_chunks):
        print(f"处理第 {idx+1} 个大块，长度：{len(chunk)}")
        
        summary = summarize_text(chunk)
        
        small_chunks = split_into_small_chunks(chunk, max_size=3000)
        
        translated_sub_blocks = []
        
        for sidx, sub_chunk in enumerate(small_chunks):
            print(f"  翻译大块{idx+1}的第 {sidx+1} 个小块，长度：{len(sub_chunk)}")
            translated_text = translate_segment(sub_chunk, summary)
            translated_sub_blocks.append(translated_text)
        
        translated_chunk = "\n".join(translated_sub_blocks)
        all_translated_texts.append(translated_chunk)
    
    final_translated_text = "\n".join(all_translated_texts)
    
    with open(output_file, "w", encoding="utf-8") as f:
        f.write(final_translated_text)
    
    print(f"翻译完成！结果已输出到：{output_file}")


if __name__ == "__main__":
    input_md = "articles/example.md"
    output_md = "articles/example_zh.md"
    main(input_md, output_md)
```


---

### 六、安装与使用

#### 1. 安装依赖

在项目根目录下，运行：

```bash
pip install -r requirements.txt
```

#### 2. 配置环境变量

根据实际需求，将 Fireworks.ai 或其他 LLM 服务的 API Key 写入环境变量或使用 `.env` 文件：

```plaintext
FIREWORKS_API_KEY=YOUR_API_KEY_HERE
FIREWORKS_API_BASE=https://api.fireworks.ai/inference/v1
```

#### 3. 运行翻译脚本

```bash
python main.py input.md -o output.md
```

- `input.md`：原文 Markdown 文件路径
- `-o, --output`：输出的翻译后 Markdown 文件
- `--large-chunk-size`：大块的最大单词数（用于摘要，默认：20000）
- `--small-chunk-size`：小块的最大单词数（用于翻译，默认：5000）
- `-l, --log-dir`：日志目录，默认 `logs`

假设有一个文章 example.md 在 articles 目录下
python main.py example.md

或者指定输出文件名（仍然保存在 articles 目录下）
python main.py example.md -o example_zh.md

基本用法（使用默认日志目录）：
python main.py input.md -o output.md

指定自定义日志目录：
python main.py input.md -o output.md -l custom_logs

使用 deepseek-v3 模型
python main.py input.md --model deepseek-v3

批量翻译
python main.py

单文件翻译
python main.py -f example.md

自定义文件夹：
python main.py --input-folder custom_input --output-folder custom_output

#### 4. 查看翻译结果

运行成功后，查看 `output.md` 即可。

---

### 七、后续改进与注意事项

- **摘要策略**：可根据文档类型、语言特点进一步优化摘要提示。
- **Markdown 解析**：若对结构保留要求较高，需使用 Markdown AST 解析，再分段处理，以防断行、代码块等格式混乱。
- **容错与重试机制**：网络调用、超时以及模型返回异常等情况需要结合实际使用场景做好重试或异常处理。
- **批量处理**：可在命令行参数中指定多个 Markdown 文件，批量翻译。
