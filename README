## 基于 LLM 的 Markdown 文本批量翻译

### 一、项目简介

本项目用于对大段 Markdown 文档进行自动化翻译，力图保留原有的 Markdown 结构。与一般翻译流程不同的是，本项目先对文本进行长文本摘要，再在实际翻译时将摘要内容作为上下文，帮助大语言模型（LLM）更好地理解文本背景并进行准确翻译。

#### 项目主要流程

- 读取并解析 Markdown 文件
- 对全文按「约 2 万词」进行首轮分段（如果原文小于 2 万词，则无需拆分），并对每段进行整体概要。
- 再次细分翻译分段：对原文（或者分段后的每部分）再按「约 2,000 词」进行二次分段，用于实际调用 LLM 翻译。在翻译时，将对应的摘要内容与 2,000 词分段的文本一并传入 LLM，帮助模型理解上下文。
- 将翻译结果拼接回原有的 Markdown 格式，形成最终输出。

#### 示例：
- **2 万词内**：不用拆分，先整体做摘要，再按 2,000 词翻译分段，翻译时带上整体摘要。
- **4 万词**：先拆分为两段，每段 2 万词；对每段做摘要，然后各自再分割成若干个 2,000 词，翻译时带上对应的 2 万词摘要。
- **6 万词**：分为三段，每段 2 万词；对每段做摘要，翻译时各段分别带其对应的摘要。

#### 单词计数说明：
- 字：character
- 词：word
- **英文文本**：按照标准英文单词进行计数（包括带连字符和数字的单词）
- **中文文本**：每个汉字视为一个词进行计数
- **混合文本**：同时支持中英文混合计数
- **特殊处理**：
  - 代码块内容不计入单词统计，避免影响分段
  - 保持 Markdown 格式标记的完整性
  - 优先在自然段落边界进行分段

---

### 二、技术选型

- **语言**：Python（建议版本 3.7+）
- **依赖库**：
  - `openai`（或自定义封装，用于调用 Fireworks.ai 等 LLM 服务）
  - `markdown`、`markdown-it-py` 或类似库（可根据需要进行文本解析和处理）
  - 其他可选的文本处理工具，如 `re`、`textwrap` 等
  - 其他必要的工具库（如 `argparse`、`dotenv` 等）

**注意**：本项目对 `openai` 模块进行了自定义封装，实际使用 Fireworks.ai 等服务。调用示例请参考下文。依然符合Openai协议。

---

### 三、分段与翻译核心思路

#### 1. 第一轮分段（约 2 万词）并摘要
- 读取整个 Markdown 文本，统计词数。
- 若小于 2 万词，则无需拆分，直接对全文做摘要。
- 若超过 2 万词，每 2 万词左右切分一次（推荐以自然段边界切分）。
- 对每个分段调用 LLM 生成段落级摘要。

#### 2. 第二轮分段（约 2,000 词）并翻译
- 对原文或对应的首轮分段再次进行精细切分，每次约 2,000 词左右。
- 每个 2,000 词分段翻译时，将「对应的摘要」和「分段文本」一起作为 Prompt 发给 LLM，以便模型在理解上下文的基础上进行翻译。
- 将翻译结果记录下来，并与原文段落/块建立对应关系。

#### 3. 拼接并保持 Markdown 结构
- 最终将所有翻译后的文本按原顺序拼接，保留标题、列表、代码块等 Markdown 结构。
- 输出完整的翻译后 Markdown 文件。

---

### 四、目录结构

```plaintext
markdown-translator/
│
├── README.md                # 项目说明文档
├── requirements.txt         # 项目依赖库
├── main.py                  # 主入口脚本，组织翻译流程
├── summarizer.py            # 摘要生成：2 万词级分段 + 调用 LLM
├── translator.py            # 翻译处理：2,000 词级分段 + 调用 LLM
├── chunker.py               # 分段策略实现
├── llm_client.py            # LLM 客户端封装 (支持多种模型)
├── to_translate/           # 存放待翻译的 Markdown 文件
├── translated/             # 存放翻译结果和原文件
├── logs/                   # 存放翻译日志
└── utils/
    ├── markdown_parser.py   # Markdown 解析与还原
    └── translation_logger.py # 翻译日志记录
```

---

### 五、示例流程代码片段

#### `summarizer.py`（简化示例）

```python
from typing import Optional
from llm_client import ZetaClient

class Summarizer:
    def __init__(self, api_key: Optional[str] = None, api_base: Optional[str] = None):
        """
        初始化摘要生成器。
        
        参数：
            api_key: LLM服务的API密钥
            api_base: LLM服务的API基础URL
        """
        self.llm_client = ZetaClient(api_key=api_key, api_base=api_base)

    def summarize(self, text: str) -> str:
        """
        对输入文本生成摘要。
        
        参数：
            text: 要生成摘要的Markdown文本
            
        返回：
            生成的摘要文本
        """
        prompt = (
            "请对以下文本生成一个简要的摘要。"
            "摘要应不超过500字，并使用中文输出：\n\n"
            f"{text}"
        )
        
        # 使用非流式输出，直接获取完整摘要
        summary = self.llm_client.generate_text(
            prompt=prompt,
            stream=False,
            model="claude-3-5-sonnet-20241022"
        )
        return summary.strip()
```

#### `translator.py`（简化示例）

```python
from typing import List, Optional, Dict, Any, Tuple
from llm_client import ZetaClient
from utils.translation_logger import TranslationLogger

class Translator:
    def __init__(self, api_key: Optional[str] = None, api_base: Optional[str] = None, default_model: str = "deepseek-v3"):
        """
        使用API凭证初始化翻译器。
        
        参数：
            api_key: 可选的API密钥。如果未提供，将尝试从环境变量获取。
            api_base: 可选的API基础URL。如果未提供，将尝试从环境变量获取。
            default_model: 默认使用的模型名称。
        """
        self.llm_client = ZetaClient(api_key=api_key, api_base=api_base)
        self.logger = TranslationLogger()
        self.default_model = default_model

    def translate_text(
        self,
        text: str,
        stream: bool = True,
        model: Optional[str] = None,
        summary: Optional[str] = None
    ) -> str:
        """
        翻译给定的文本，同时保持Markdown格式。
        
        参数：
            text: 要翻译的文本（Markdown格式）
            stream: 是否使用流式模式
            model: 使用的模型名称，如果未提供则使用默认模型
            summary: 可选的上下文摘要
            
        返回：
            保持Markdown格式的翻译后文本
        """
        model = model or self.default_model
        system_prompt, user_prompt = self._build_translation_prompts(text, summary)
        response_format = self._get_response_format(model)

        if stream:
            response_iterator = self.llm_client.generate_text(
                prompt=user_prompt,
                stream=True,
                model=model,
                system_prompt=system_prompt,
                response_format=response_format
            )
            final_text = self._process_stream_response(response_iterator)
            return final_text
        else:
            return self.llm_client.generate_text(
                prompt=user_prompt,
                stream=False,
                model=model,
                system_prompt=system_prompt,
                response_format=response_format
            )

    def translate_batch(
        self,
        texts: List[str],
        model: Optional[str] = None,
        summary: Optional[str] = None
    ) -> List[str]:
        """
        批量翻译文本段落。使用 self.translate_text。
        
        参数：
            texts: 要翻译的文本段落列表
            model: 使用的模型名称，如果未提供则使用默认模型
            summary: 可选的上下文摘要
            
        返回：
            翻译后的文本段落列表
        """
        model = model or self.default_model
        total = len(texts)
        results = []
        
        for i, text in enumerate(texts, 1):
            result = self.translate_text(text, model=model, summary=summary)
            results.append(result)
            
        return results
```

#### `chunker.py`（简化示例）

```python
from typing import List
import re

class Chunker:
    def __init__(self, large_chunk_size: int = 20000, small_chunk_size: int = 2000):
        """
        使用最大块大小初始化分段器。
        
        参数：
            large_chunk_size: 大块的最大单词数（用于摘要）
            small_chunk_size: 小块的最大单词数（用于翻译）
        """
        self.large_chunk_size = large_chunk_size
        self.small_chunk_size = small_chunk_size

    def split_for_summary(self, text: str) -> List[str]:
        """
        将输入文本分割成大块（约20,000词），用于生成摘要。
        """
        return self._split_text(text, self.large_chunk_size)

    def split_for_translation(self, text: str) -> List[str]:
        """
        将输入文本分割成小块（约2,000词），用于翻译。
        """
        return self._split_text(text, self.small_chunk_size)

    def _split_text(self, text: str, max_size: int) -> List[str]:
        """
        将输入文本分割成块，同时保持Markdown结构。
        """
        # 如果文本较短，直接返回
        if self._count_words(text) < max_size * 0.8:
            return [text]

        # 按标题分割，保持"标题+内容"作为最小单元
        raw_sections = re.split(r'(#{1,6}\s[^\n]+\n)', text)
        section_blocks = []
        
        for block in raw_sections:
            if block.strip():
                section_blocks.append(block)

        # 智能合并section_blocks
        chunks = []
        current_chunk = ""
        current_word_count = 0

        for block in section_blocks:
            block_word_count = self._count_words(block)
            if current_word_count + block_word_count <= max_size:
                current_chunk += block
                current_word_count += block_word_count
            else:
                if current_chunk:
                    chunks.append(current_chunk)
                current_chunk = block
                current_word_count = block_word_count

        if current_chunk:
            chunks.append(current_chunk)

        return chunks
```

#### `main.py`：主流程示例

```python
from chunker import Chunker
from translator import Translator
from summarizer import Summarizer

def translate_file(
    input_file: str,
    model: str,
    output_file: str,
    large_chunk_size: int = 20000,
    small_chunk_size: int = 2000,
    api_key: Optional[str] = None,
    api_base: Optional[str] = None,
    log_dir: str = "logs"
) -> None:
    """
    翻译Markdown文件，同时保持其结构。
    使用两级分段策略：先按大块生成摘要，再按小块进行翻译。
    """
    # 初始化组件
    chunker = Chunker(large_chunk_size=large_chunk_size, small_chunk_size=small_chunk_size)
    translator = Translator(api_key=api_key, api_base=api_base)
    summarizer = Summarizer(api_key=api_key, api_base=api_base)
    translator.logger.log_dir = log_dir

    # 读取输入文件
    with open(input_file, 'r', encoding='utf-8') as f:
        content = f.read()

    # 第一级分段（约20,000词/段）
    word_count = chunker._count_words(content)
    if word_count <= large_chunk_size:
        large_chunks = [content]
    else:
        large_chunks = chunker.split_for_summary(content)

    # 处理每个大段（生成摘要、二次分段、翻译）
    all_translated_chunks = []
    for i, large_chunk in enumerate(large_chunks, 1):
        # 1. 为当前大块生成摘要
        summary = summarizer.summarize(large_chunk)
        
        # 2. 将大块分成小块（约2,000词）
        small_chunks = chunker.split_for_translation(large_chunk)
        
        # 3. 使用摘要作为上下文翻译所有小块
        translated_chunks = translator.translate_batch(small_chunks, summary=summary)
        all_translated_chunks.extend(translated_chunks)

    # 合并所有翻译结果
    final_text = '\n\n'.join(all_translated_chunks)

    # 保存结果
    with open(output_file, 'w', encoding='utf-8') as f:
        f.write(final_text)
```

---

### 六、安装与使用

#### 1. 安装依赖

在项目根目录下，运行：

```bash
pip install -r requirements.txt
```

#### 2. 配置环境变量

根据实际需求，将 Fireworks.ai 或其他 LLM 服务的 API Key 写入环境变量或使用 `.env` 文件：

```plaintext
FIREWORKS_API_KEY=YOUR_API_KEY_HERE
FIREWORKS_API_BASE=https://api.fireworks.ai/inference/v1
```

#### 3. 运行翻译脚本

```bash
python main.py input.md -o output.md
```

- `input.md`：原文 Markdown 文件路径
- `-o, --output`：输出的翻译后 Markdown 文件
- `--large-chunk-size`：大块的最大单词数（用于摘要，默认：20000）
- `--small-chunk-size`：小块的最大单词数（用于翻译，默认：5000）
- `-l, --log-dir`：日志目录，默认 `logs`

假设有一个文章 example.md 在 articles 目录下
python main.py example.md

或者指定输出文件名（仍然保存在 articles 目录下）
python main.py example.md -o example_zh.md

基本用法（使用默认日志目录）：
python main.py input.md -o output.md

指定自定义日志目录：
python main.py input.md -o output.md -l custom_logs

使用 deepseek-v3 模型
python main.py input.md --model deepseek-v3

批量翻译
python main.py

单文件翻译
python main.py -f example.md

自定义文件夹：
python main.py --input-folder custom_input --output-folder custom_output

#### 4. 查看翻译结果

运行成功后，查看 `output.md` 即可。

---

### 七、后续改进与注意事项

- **摘要策略**：可根据文档类型、语言特点进一步优化摘要提示。
- **Markdown 解析**：若对结构保留要求较高，需使用 Markdown AST 解析，再分段处理，以防断行、代码块等格式混乱。
- **容错与重试机制**：网络调用、超时以及模型返回异常等情况需要结合实际使用场景做好重试或异常处理。
- **批量处理**：可在命令行参数中指定多个 Markdown 文件，批量翻译。
